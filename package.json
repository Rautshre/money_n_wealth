{
  "name": "money_n_wealth",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "dangerous-html": "0.1.13",
    "firebase": "^11.8.0",
    "prop-types": "15.7.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-helmet": "^7.6.1",
    "react-router-dom": "^7.6.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && craco start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && craco build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && craco test --env=jsdom",
    "eject": "craco eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "engines": {
    "node": ">=18.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "react-scripts": "^3.0.1"
  }
}
